-- C# default indentation. Inside BTNet the built-in editorconfig support reads
-- .editorconfig (indent_size = 4) and applies the same values; this is the
-- fallback for C# files edited outside a repo that ships one.
vim.opt_local.shiftwidth = 4
vim.opt_local.tabstop = 4
