-- The Which Key config was getting too large, so I split it into two files,
-- one to configure the plugin options themselves and another to define the
-- keymappings
require('setup.which-key.config')
require('setup.which-key.keymappings')
